#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdbool.h>

void print_array(int *arr, size_t sz);
void bubble_sort(int *array, size_t size);

#endif
